import { MetadataConfig } from '@/common/types/metadata.types';

/**
 * Web metadata configuration
 */
export const metadataConfig: MetadataConfig = {
  title: 'QR Generator ~ Permanent QR codes that never expire',
  description:
    'Generate modern QR codes that never expire. The data is encoded directly into the code — no backend, no tracking, no expiration. Free and easy to use!',
  keywords: ['qr generator', 'qr code', 'wifi qr code', 'vcard qr', 'free tool', 'permanent qr'],
};
